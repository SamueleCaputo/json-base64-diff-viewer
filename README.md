# json-base64-diff-viewer
A GUI tool for comparing JSON and Base64 JSON with smart diffing. It detects list keys to align objects even when order changes, highlights exact modified lines, shows missing or extra items, and displays normalized JSON side-by-side in a clear WinMerge-like view.
